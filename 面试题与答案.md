# 面试官高频问题与标准答案（RAG 知识库问答项目）

> 配合 `项目讲解与面试宝典.md` 使用。每题给出**标准答案**与**加分点**，
> 加分点用于在被追问时展现深度。建议先背熟"标准答案"，再理解"加分点"。

---

## 一、基础概念题

### Q1：什么是 RAG？为什么不直接用大模型？
**标准答案**：RAG（Retrieval-Augmented Generation，检索增强生成）先根据用户问题从知识库检索相关片段，
再让大模型**只基于这些片段**回答。纯大模型有三个硬伤：知识有截止日期、会产生幻觉、不知道企业内部私有数据。
RAG 相当于给模型"开卷考试"，用外部知识弥补这三点。

**加分点**：RAG 的代价是链路更长（检索+重排+拼接），延迟和工程复杂度更高；
纯大模型适合通用闲聊，RAG 适合"必须基于指定资料作答"的场景。

### Q2：RAG 和 Fine-tuning（微调）有什么区别？怎么选？
**标准答案**：微调是"把知识塞进模型权重"，RAG 是"把知识放在外部随时查"。
知识频繁更新时 RAG 只需改知识库、不用重训；微调成本高、更新慢，但能学到特定"风格/能力"。
一般先用 RAG 解决"知不知道"，再用微调解决"会不会某种表达"。

**加分点**：RAG 可解释性强（能给出引用来源），微调是黑盒；生产里常以 RAG 为主、微调为辅。

### Q3：什么是 Embedding？为什么需要它？
**标准答案**：Embedding 是把文本映射成一串固定维度的数字向量，语义相近的文本向量在空间中距离更近。
有了向量，就能用"距离/余弦相似度"衡量两段文字是否相关，这是向量检索的基础。

**加分点**：本项目用 `bge-small-zh-v1.5` 并做 L2 归一化，使余弦相似度等价于点积，
检索时直接按点积排序即可，且分数落在 0~1，便于设阈值过滤噪声。

### Q4：什么是向量数据库？和关系型数据库区别？
**标准答案**：向量数据库专为"按相似度找最近邻"优化（ANN 近似最近邻索引，如 HNSW）。
关系型数据库擅长精确匹配（WHERE id=1），不擅长"意思最像的十条"。本项目用轻量的 ChromaDB 做本地向量存储。

**加分点**：大规模场景会换 Milvus / Elasticsearch；向量库通常配合元数据过滤（如限定某文档）。

---

## 二、架构与设计题

### Q5：你的检索为什么既要向量又要 BM25？
**标准答案**：两者互补。向量检索擅长语义相似（"怎么辞职"能匹配"离职流程"），但弱于专有名词
（型号、人名、术语）；BM25 基于词频做字面精确匹配，正好补齐向量短板。双路召回再融合，覆盖更全面。

**加分点**：纯向量检索在"用户抄了文档原话但换了个说法"时很强，但在"搜一个具体产品编号"时容易翻车，
这正是 BM25 的强项。

### Q6：RRF 融合是什么？为什么用排名而不是分数？
**标准答案**：RRF（Reciprocal Rank Fusion，倒数排名融合）公式 `score = Σ 1/(k+rank)`，
把多路召回结果按各自排名累加融合。不同检索器分数量纲不同（余弦 0~1 vs BM25 任意值），
直接相加会被大量纲的一方主导；只用排名就天然兼容多路、无需归一化。

**加分点**：k 是常数（本项目 60），越大各路差异越平滑；RRF 是工业界混合检索的常用做法，
简单鲁棒，不需要训练。

### Q7：什么是重排（Rerank）？和前面的向量召回有什么区别？
**标准答案**：前面向量召回是"双塔"模型，查询和文档分别编码再算相似，快但精度有限，只用于粗筛出候选；
重排是"单塔"CrossEncoder，把"查询+文档"拼接一起逐对精算相关性，更准但慢，所以只用在召回后的少量候选上（如 20 选 4）。

**加分点**：双塔可离线建索引、毫秒级；CrossEncoder 要实时成对推理，开销大，必须放在召回之后做精筛，
这是"召回-重排"两段式架构的核心思想。

### Q8：什么是父子块（Small-to-Big）？解决什么问题？
**标准答案**：小块（如 500 字）建索引保证检索精度，命中后返回所属大块（如 1500 字）作为上下文，
保证答案完整不被切断。父块存 SQLite，不进向量库以免污染召回。

**加分点**：如果不分块直接整篇检索，长文档里相关句被无关句稀释，相似度打分偏低容易漏召；
分太小则上下文断裂、答案残缺。Small-to-Big 是兼顾"检索精度"与"上下文完整"的折中。

### Q9：多轮对话你怎么处理的？
**标准答案**：用 LLM 做查询改写，把含指代的追问（"他的学历？"）补成独立查询（"周阳的学历？"）再去检索；
改写失败自动降级用原问题，不影响主流程。同时前端维护最近若干轮历史回传后端。

**加分点**：也可上 Multi-Query（把一个问题扩成多个表述并行检索提升召回），但会增加检索开销，
本项目默认关、按需开。

---

## 三、技术细节深挖题

### Q10：怎么防止大模型幻觉？
**标准答案**：三道防线——① 提示词强约束"只依据上下文、无依据输出固定兜底话术"；
② 检索侧用 CrossEncoder 重排只留高相关片段，减少噪声喂给模型；③ 库外问题统一拒答，
而非让模型自由发挥。

**加分点**：还可做"答案事实校验"（用 LLM 逐句判断能否由上下文推出，即 Faithfulness 指标），
本项目评估脚本已内置忠实度计算。

### Q11：SSE 和 WebSocket 有什么区别？为什么用 SSE？
**标准答案**：SSE 是服务端单向推送（适合问答流），基于普通 HTTP、自动重连、实现简单；
WebSocket 是双向全双工。问答场景只需要"服务端推、客户端收"，SSE 更轻量，
本项目在响应头加 `X-Accel-Buffering: no` 关闭反代缓冲保证流式不被攒批。

**加分点**：SSE 走标准 HTTP，易被 Nginx 等代理、CDN 兼容；WebSocket 常需额外升级握手与心跳。

### Q12：为什么用本地模型 Ollama？有什么取舍？
**标准答案**：数据不出内网、无 API 费用、无外网依赖，适合企业私密知识库。
代价是推理速度依赖本地显卡，且要自己维护模型加载（本项目用 `keep_alive` 让模型常驻显存避免冷启动）。

**加分点**：也可一行配置切到 DeepSeek 等 OpenAI 兼容云端模型（`LLM_PROVIDER=openai`），代码无需改动，体现可插拔设计。

### Q13：ChromaDB 存的向量空间用的是什么距离？为什么？
**标准答案**：余弦空间（`hnsw:space: cosine`）。因为 embedding 做了 L2 归一化，
余弦相似度等价于点积，分数落在 0~1，便于用 `SCORE_THRESHOLD` 过滤低相关片段、降低噪声进上下文。

---

## 四、性能与优化题

### Q14：系统做了哪些性能优化？
**标准答案**：① embedding 启动预热，避免首问卡顿；② 重排模型懒加载，首次问答才进显存；
③ 检索/生成等阻塞操作放进线程池（`run_in_threadpool`），不卡 FastAPI 事件循环；
④ `keep_alive` 让模型常驻显存避免冷启动；⑤ BM25 语料缓存，写入时失效。

**加分点**：CPU 环境建议关重排（否则每次问答额外秒级开销）；追求延迟可降 `CANDIDATE_K`；
追求准确率可开 Multi-Query 并增大候选数。

### Q15：为什么检索/生成要放进线程池？
**标准答案**：FastAPI 基于异步事件循环，embedding/重排/LLM 推理是 CPU 或 IO 密集的阻塞调用，
直接跑会阻塞整个事件循环、拖垮并发。用 `run_in_threadpool` 交给线程池执行，
保持事件循环响应其他请求。`StreamingResponse` 的生成器也会被 Starlette 自动线程池化。

### Q16：如果知识库很大（百万级分块）会有什么瓶颈？怎么演进？
**标准答案**：当前 BM25 从 ChromaDB 拉全量文档在内存建索引，万级以内无压力；
更大规模应改 Elasticsearch 或 Milvus 的稀疏向量，把 BM25 也下沉到专门引擎。
多 worker 部署时各进程 BM25 缓存不同步，需改为单 worker 或外接向量库。

---

## 五、故障与容错题（讲"难点"时用）

### Q17：如果重排模型加载/推理失败会怎样？
**标准答案**：优雅降级——捕获异常后自动退回"融合排序结果"，服务不中断。
这是系统多处采用的容错策略（BM25 缺失、改写失败同理回退）。

**加分点**：降级不是掩盖错误，而是保证"主链路可用"；同时打日志告警，便于后续排查。
设计上坚持"单点故障不拖垮全链"。

### Q18：讲一个你联调时遇到的真实坑。
**标准答案**：Ollama 的 `keep_alive` 配成字符串 `"-1"` 触发 400
`time: missing unit in duration "-1"`。根因是 Ollama 该字段要数字 `-1`（永久驻留）而非字符串；
字符串会被 Go 的 `time.ParseDuration` 当作带单位duration解析，因无单位报错。
修复：在配置层把纯数字转成 int、带单位字符串（如 `5m`）原样保留。

**加分点**：这反映"联调第三方服务要看清字段类型约定"；`-1` 和 `"-1"` 对 Ollama 是完全不同的语义，
不能想当然认为"配置能读出来就对了"。

---

## 六、开放 / 场景题

### Q19：如果用户问的问题知识库里没有，系统会怎样？
**标准答案**：检索阶段可能召回不到高相关片段（被阈值过滤），重排后上下文为空或无关；
提示词强制模型"无依据就输出固定话术"，于是返回"知识库中暂无相关信息，无法回答。"，
而不是编造。评估脚本里也有"拒答正确性"指标专门测这类。

**加分点**：可进一步做"置信度阈值"——若重排最高分低于某值直接拒答，比只看"有没有召回"更稳。

### Q20：怎么衡量你这个 RAG 系统好不好？
**标准答案**：分两层——检索层看命中率/召回率/MRR（期望来源是否出现、排名多前）；
生成层看忠实度（答案能否由上下文推出，衡量幻觉）、相关性、与标准答案的语义相似度。
本项目内置黄金测试集 + 评估脚本，也可接 RAGAS 框架输出 faithfulness / answer_relevancy 等指标。

**加分点**：没有评估集的 RAG 调优是"盲调"。先建黄金集，每次改参数跑一遍对比，才有依据。

### Q21：如果让你再加一个能力，你会加什么？
**标准答案（选答其一即可）**：
- **表格/扫描件解析**：当前主要处理纯文本 PDF，表格和图片型 PDF 易丢信息，可接 OCR / 多模态模型；
- **会话持久化**：当前历史由前端携带、服务端不存，可接 Redis 做多端同步；
- **鉴权与限流**：当前接口无鉴权，仅适合内网；加 API Key / 限流可上生产；
- **异步索引 + 进度回调**：大文件上传时给出进度，避免前端长时间等待。

---

## 附：被追问时的"万能接话"框架

当被问到"为什么选 X 技术"，用 **「痛点 → 选型 → 代价」** 三句式：
1. 痛点：原来有什么问题；
2. 选型：X 怎么解决这个问题；
3. 代价：X 带来了什么新成本（说明你权衡过，不是盲选）。

例：「为什么用 CrossEncoder 重排？」→ 向量召回精度有限、噪声进上下文致幻觉（痛点）；
CrossEncoder 逐对精算只留最相关（选型）；但它实时推理慢，所以只放在召回后少量候选上（代价）。
