# 企业级 RAG 知识库问答系统 —— 项目讲解（小白版 / 简历向）

> 本文档面向想把这个项目写进简历、准备面试的同学。从"这是什么"讲到"代码里怎么落地"，
> 最后给出可直接复制的简历描述。配合 `面试题与答案.md` 一起看效果最佳。

---

## 一、一句话讲清楚（简历摘要用）

> 基于 **FastAPI + LangChain + ChromaDB + 本地大模型（Ollama / qwen2.5）** 搭建的检索增强生成（RAG）系统。
> 支持上传 PDF/Word/TXT 文档并自动建索引，用户提问后通过
> 「**查询改写 → 向量 + BM25 混合召回 → RRF 融合 → CrossEncoder 重排 → 父子块展开 → 大模型流式生成**」
> 全链路，返回**带引用溯源**的答案，全程本地化、无外网依赖。

---

## 二、为什么需要它？（建立"痛点"认知）

大语言模型（LLM）有两个天生缺陷：

1. **知识有截止日期**，不知道你公司内部的私密文档（合同、手册、简历）。
2. **会一本正经地胡说八道（幻觉）**。

解决思路就是 **RAG（Retrieval-Augmented Generation，检索增强生成）**：
> 先去你的资料库里**找相关内容**，"喂"给大模型，让它**只根据这些内容**回答。

本项目就是把这套工业级 RAG 链路完整实现了一遍。

---

## 三、整体架构（一张图看懂）

```
            上传文档                用户提问
   ┌──────────────┐        ┌─────────────────────────────────────┐
   │  PDF/Word/TXT │        │           提问进入后端               │
   └──────┬───────┘        │                                       │
          │ 加载+切分       │  ① 查询改写（多轮指代消解）           │
          ▼                │  ② 混合召回：                         │
   ┌──────────────┐        │     ├─ 向量召回（语义相似）           │
   │  父子块切分   │        │     └─ BM25 召回（字面匹配）         │
   │ 父块→SQLite   │        │  ③ RRF 融合（合并两路排名）          │
   │ 子块→向量库   │        │  ④ CrossEncoder 重排（精筛）         │
   └──────┬───────┘        │  ⑤ 父子块展开（小块召回→父块生成）    │
          │                │  ⑥ 拼提示词 → 大模型流式生成          │
          ▼                │  ⑦ SSE 推送：引用先行 + 打字机输出     │
   ┌──────────────┐        └─────────────────────────────────────┘
   │ ChromaDB 向量库│                    │
   │ + ParentStore │◄─────── 检索阶段回取上下文
   └──────────────┘
```

- 前端：独立 **Streamlit** 应用（打字机输出 + 引用高亮 + 多轮对话 + 知识库管理）。
- 后端：**FastAPI**，提供 REST 接口与 SSE 流式接口。
- 前后端通过 HTTP/SSE 解耦，可分开部署。

---

## 四、跟着一次提问，走完整个流程（重点）

### 第 0 步：上传文档（建库阶段）

文档进来后做两件事：
- **加载**：用对应 Loader 把 PDF/Word/TXT 读成纯文本（`app/utils/file_utils.py` 按扩展名分发）。
- **切分（Chunking）**：长文档不能整篇喂给模型，要切成小块。本项目用**父子块（Small-to-Big）**：

```python
# app/services/rag_engine.py :: _split_parent_child
# 先切大块（父，1500 字，用于生成），再在每个父块内切小块（子，500 字，用于召回）
parents = self.parent_splitter.split_documents(docs)   # 父块
children = self.child_splitter.split_documents(parents)  # 子块，天然不跨父块边界
```

**小白比喻**：把一本书先裁成"章"（父块），每章再裁成"小节"（子块）。
检索时用"小节"精确定位，回答时把整个"章"给模型看，保证上下文完整不割裂。

### 第 1 步：向量化（Embedding）——这套系统的"地基"

切好的文本片段要变成计算机能比较"相似度"的形式，即**向量（一串数字）**。
意思越相近的文字，向量在空间里离得越近。

- 模型：**BAAI/bge-small-zh-v1.5**（中文 embedding，本地运行）。
- 关键细节：embedding 做了**归一化**，向量库用**余弦空间**（`hnsw:space: cosine`），
  所以"相似度分数"直接就是余弦相似度，取值 0~1。

### 第 2 步：存入向量库

向量存进 **ChromaDB**（轻量级本地向量数据库）；父块文本不进向量库（避免污染召回精度），
改存 **SQLite**（`ParentStore`，见 `app/services/store.py`）。

### 第 3 步：用户提问 → 查询改写

很多 RAG 项目忽略、但**决定多轮对话好不好用**的一环。

**小白比喻**：你问完"这份简历是谁的？"之后追问"**他**的学历呢？"——
"他"指代谁？直接拿"他的学历"去搜必然搜不到。本项目用大模型把追问**改写**成独立可搜的查询："周阳的学历"。

代码里做了**分级降级**：改写失败（模型异常）就静默退回原问题，不影响主流程。

### 第 4 步：混合召回（向量 + BM25）

两套互补的检索同时跑：

| 方式 | 擅长 | 比喻 |
| --- | --- | --- |
| **向量召回** | 语义相似（"怎么辞职"能匹配"离职流程"） | 理解意思 |
| **BM25 召回** | 字面精确（人名、型号、专有名词） | 像 Ctrl+F 关键词查找 |

BM25 用 jieba 做中文分词（`app/utils/text_utils.py`）：英文数字整体成词，中文片段用 jieba 精确切分。

### 第 5 步：RRF 融合

两套召回分数"量纲不同"（一个是余弦 0~1，一个是 BM25 任意值），不能直接相加。用 **RRF（倒数排名融合）**：

```python
# app/services/retrieval.py :: _rrf_merge
# score = Σ 1/(k + rank)，只用排名、不看原始分数
rrf[key] = rrf.get(key, 0.0) + 1.0 / (settings.RRF_K + rank + 1)
```

**小白比喻**：两个排行榜（语义榜、关键词榜），不比谁分数高，只看"谁排得靠前"。
排第 1 名加的分多，排第 10 名加的分少，把两榜名次合成一个新排名。天然兼容不同量纲。

### 第 6 步：重排（CrossEncoder）——质量的关键

融合后的候选（如 20 条）再交给 **CrossEncoder 重排模型**（BAAI/bge-reranker-base）精筛：

```python
# app/services/rerank.py :: rerank
pairs = [[query, doc.page_content] for doc, _ in hits]
scores = model.predict(pairs)   # 把"问题+片段"成对送进去打分
# 按分数排序，低于阈值（logits -5.0）的剔除
```

**小白比喻**：前面是"海选"（谁沾边都要），重排是"终面考官"——
把"问题+某段文字"放一起，让模型判断"这段到底和这问题有多相关"，只留最相关的几条进上下文。
这极大降低噪声、抑制幻觉。

### 第 7 步：父子块展开

重排命中的是"子块（小节）"，但回答需要完整上下文，所以按 `parent_id` 回取"父块（章）"：

```python
# app/services/rag_engine.py :: expand_to_parents
# 命中子块后，回取所属父块作为 LLM 上下文；同一父块多个子块只保留一次（保留最高分）
```

### 第 8 步：拼提示词 → 大模型生成

把召回内容拼进提示词，并**强制约束**：只依据上下文回答，没依据就统一说
"知识库中暂无相关信息，无法回答。"——这就是**反幻觉**的核心设计。

### 第 9 步：流式返回（SSE）+ 引用溯源

答案不是等全部生成完才给，而是**逐字流式**返回（打字机效果）；而且**引用来源先于答案推送**，
前端可以先渲染引用卡片。

```python
# app/api/v1/endpoints/chat.py :: chat_stream / event_generator
# 事件序列：references（引用先行）→ token（答案增量）→ done（完成）→ error（异常）
yield _sse(event, item)   # SSE 格式：event: xxx\ndata: {...}\n\n
```

**SSE（Server-Sent Events）** 是 HTTP 上的单向流式推送协议，比 WebSocket 更轻量，
天然适配"服务端推、客户端收"的问答场景。

---

## 五、关键技术名词弹药库（面试被问到能接住）

| 名词 | 一句话 | 本项目落地 |
| --- | --- | --- |
| **RAG** | 先检索再生成，给模型"开卷考试" | 全链路 |
| **Embedding** | 把文字变成可计算距离的数字向量 | bge-small-zh-v1.5，归一化+余弦 |
| **向量数据库** | 按"意思相近"快速找邻居 | ChromaDB，余弦空间 |
| **Chunking** | 长文切块才能精准检索 | 父子块 1500/500 |
| **Small-to-Big** | 小块召回、父块生成，兼顾精度与完整 | SQLite 存父块 |
| **BM25** | 基于词频的关键字精确匹配 | jieba 中文分词 |
| **RRF** | 只用排名融合多路结果 | `1/(k+rank)` 累加，k=60 |
| **CrossEncoder 重排** | 逐对精算"问题与片段相关性" | bge-reranker-base，logits 阈值 -5 |
| **查询改写** | 多轮指代消解，追问变独立查询 | LLM 改写，失败降级 |
| **SSE** | HTTP 流式推送，打字机效果 | references/token/done |
| **反幻觉** | 约束只依上下文作答 | Prompt 强约束 + 统一兜底话术 |
| **本地化 LLM** | 模型跑在自己机器，数据不出内网 | Ollama + qwen2.5:7b |

**额外亮点（面试官会眼前一亮）**：
- **全链路本地化、无外网/无 API Key 依赖**——适合内网、涉密场景。
- **优雅降级**：BM25 缺失、重排失败、改写失败，都自动回退上一环节，不死链。
- **懒加载**：Embedding 启动预热，重排模型首次问答才加载，不拖慢启动。
- **非阻塞**：检索/生成等阻塞操作放进线程池（`run_in_threadpool`），不卡 FastAPI 事件循环。
- **可量化评估**：内置黄金测试集 + RAGAS 指标（忠实度/相关性/召回）。

---

## 六、简历怎么写（可直接复制）

**项目名**：企业级 RAG 知识库问答系统（Python / FastAPI / LangChain）

**项目描述**：
独立设计并实现了基于检索增强生成（RAG）的企业知识库问答系统。后端采用 FastAPI 构建
RESTful 与 SSE 流式接口，结合 LangChain 编排「查询改写 → 向量+BM25 混合召回 → RRF 融合 →
CrossEncoder 重排 → 父子块展开 → 大模型生成」全链路；使用 ChromaDB 做向量存储、SQLite 管理
父子块，本地 Embedding 与 Ollama 大模型实现全链路离线运行。前端基于 Streamlit 实现带引用溯源
与打字机效果的对话界面。系统具备混合检索、重排精筛、多轮指代消解、反幻觉约束与多级优雅降级能力。

**技术栈**：Python · FastAPI · LangChain · ChromaDB · SQLite · BM25 · CrossEncoder · RAG · SSE · Streamlit · Ollama

**个人职责 / 亮点（挑你做的写）**：
- 设计 Small-to-Big 父子块检索，解决长答案被切块截断问题；
- 实现向量+BM25 双路混合召回与 RRF 融合，提升专有名词命中率；
- 引入 CrossEncoder 重排，显著降低噪声与幻觉；
- 基于 SSE 实现流式输出与引用先行，优化前端体验；
- 设计查询改写模块，修复多轮对话指代失效。

---

## 七、真实踩坑故事（面试谈"难点"时可用）

**现象**：前端一提问就报 400：
`RuntimeError: 模型调用失败... time: missing unit in duration "-1" (status code: 400)`

**根因**：`.env` 里 `OLLAMA_KEEP_ALIVE=-1` 被 `config.py` 读成**字符串** `"-1"`，
原样传给 Ollama。Ollama 服务端用 Go 的 `time.ParseDuration()` 解析 `keep_alive`，
字符串 `"-1"` 没有时间单位（合法写法是 `"5m"`/`"1h"`），于是报
`time: missing unit in duration "-1"`。

**修复**：在 `config.py` 增加 `_get_keep_alive()`，把纯数字（含负号，如 `-1`/`3600`）转成
`int`，带单位的字符串（如 `5m`）原样保留。这样 `-1` 以数字形式传给 Ollama，
符合其"整数秒、-1 表示永久驻留"的接口约定。

**收获（面试话术）**：联调第三方服务时要看清新接口的字段类型约定；
"配置都能跑通"不等于"类型正确"，字符串 `-1` 和数字 `-1` 对 Ollama 是完全不同的语义。
